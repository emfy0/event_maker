<!DOCTYPE html>
<html>
  <head>
    <title>Events</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <script src="https://api-maps.yandex.ru/2.1/?apikey=<%= Rails.application.credentials.yandex_maps_api_key %>&lang=ru_RU" type="text/javascript"></script>
  </head>

  <body>
    <div class="navbar navbar-expand navbar-dark bg-dark mb-2">
      <div class="container-fluid container">

        <%= link_to t('app.name'), root_path, class: 'navbar-brand' %>

        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
          <ul class="navbar-nav ">
            <li class="nav-item">
              <%= link_to t('app.github'), 'https://github.com/emfy0/event_maker', class: "btn btn-outline-primary ms-2", target: "_blank" %>
            </li>
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to t('user.profile'), current_user, class: "btn btn-outline-primary ms-2"%>
              </li>
              <li class="nav-item">
                <%= button_to t('user.log_out'), destroy_user_session_path, method: :delete, class: "btn btn-outline-primary ms-2"%>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to t('user.log_in'), new_user_session_path, class: "btn btn-outline-primary ms-2" %>
              </li>
              <li class="nav-item">
                <%= link_to t('user.register'), new_user_registration_path, class: "btn btn-outline-primary ms-2" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <%= bootstrap_flash(flash) %>
      <div class="row">
        <div class="col-lg-12">
          <%= yield %>
        </div>
      </div>

    </div>
    <footer class="footer">
      <div class="container border-top">
        <p class="text-center text-muted mt-2">&copy; <%= t 'app.company' %> 2022</p>
      </div>
    </footer>
  </body>
</html>
