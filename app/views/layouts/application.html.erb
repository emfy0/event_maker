<!DOCTYPE html>
<html>
  <head>
    <title>Events</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
  <div class="navbar navbar-expand navbar-dark bg-dark mb-2">
    <div class="container-fluid container">

      <%= link_to t('app.name'), root_path, class: 'navbar-brand' %>

      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav ">
          <% if user_signed_in? %>
            <li class="nav-item"><%= link_to 'Профиль', current_user, class: "btn btn-outline-primary ms-2"%></li>
            <li class="nav-item"><%= button_to 'Выйти', destroy_user_session_path, method: :delete, class: "btn btn-outline-primary ms-2"%></li>
          <% else %>
            <li class="nav-item"><%= link_to 'Войти', new_user_session_path, class: "btn btn-outline-primary ms-2" %></li>
            <li class="nav-item"><%= link_to 'Регистрация', new_user_registration_path, class: "btn btn-outline-primary ms-2" %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>

  <div class="container">
    <% if flash[:notice].present? %>
      <div class="alert alert-dark alert-dismissible fade show" role="alert">
        <%= flash[:notice] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
        </button>
      </div>
    <% elsif flash[:alert].present? %>
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <%= flash[:alert] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
        </button>
      </div>
    <% end %>
    <div class="row">
      <div class="col-lg-9">
        <%= yield %>
      </div>
      <div class="col-lg-3">
      </div>
    </div>

    <footer class="fixed-bottom">
    <div class="container border-top">
      <p class="text-center text-muted mt-2">&copy; <%= t 'app.company' %> 2022</p>
    </div>
    </footer>
  </div>
  </body>
</html>
